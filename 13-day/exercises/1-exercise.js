/*
   Write a function calculateProduct that takes two parameters,
   both of which are strings representing numbers.
   The function should explicitly convert these strings to numbers and then return their product.

   Examples:
    calculateProduct("10", "5");    // Expected: 50
    calculateProduct("abc", "5");   // Expected: NaN (due to invalid conversion)
*/

