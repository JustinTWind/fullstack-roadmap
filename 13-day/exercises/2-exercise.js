/*
   Write a function performMixedOperation that takes two parameters: a string representing a number, and a boolean.
   The function should return the result of subtracting the boolean from the string.

   Examples:
    performMixedOperation("10", true);   // Expected: 9
    performMixedOperation("abc", true);  // Expected: NaN
*/


