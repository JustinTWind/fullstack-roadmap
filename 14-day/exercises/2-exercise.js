/*
  Write a function createUpperCaseGreeting that takes two string parameters,
  firstName and lastName.
  The function should concatenate them, separated by a single space,
  convert the entire resulting full name to uppercase,
  and return the formatted full name.
*/
